<?php
  
// Get the user id 
$user_id = $_REQUEST['user_id'];
  
// Database connection
$con = mysqli_connect("localhost","root","","advocate");
  
if ($user_id !== "") {
      
    // Get corresponding first name and 
    // last name for that user id    
    $query = mysqli_query($con, "SELECT Clientid,Clientname 
     FROM caseschdeule WHERE id='$user_id'");
//  echo  $query;
    $row = mysqli_fetch_array($query);
  
    // Get the first name
    $first_name = $row["Clientid"];
	 $age = $row["Clientname"];
	//  $city = $row["Cases"];
  
    // Get the first name
  //  $last_name = $row["last_name"];
}
  
// Store it in a array
$result = array("$first_name","$age");
  
// Send in JSON encoded form
$myJSON = json_encode($result);
echo $myJSON;
?>